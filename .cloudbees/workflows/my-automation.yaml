apiVersion: automation.cloudbees.io/v1alpha1
kind: workflow
name: My automation
on:
  push:
    branches:
      - "**"
jobs:
  build:
    steps:
      - name: Say hello
        uses: docker://golang:1.20.3-alpine3.17
        shell: sh
        run: |
          echo "hello world"
  job1:
    needs:
      - build
    environment: pod-2-staging
    steps:
      - name: sayhello
        uses: cloudbees-io/checkout@v1
